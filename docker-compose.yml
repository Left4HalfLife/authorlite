services:
  web:
    build: .
    ports:
      - "3846:3846"
    volumes:
      # Persist data directory across container restarts
      - ./data:/app/data
    # Run the app as the dedicated non-root user created in the image.
    # Use the .env file located in this directory.
    env_file:
      - ./.env